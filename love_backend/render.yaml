# Render Blueprint - Infrastructure as Code
# Deploy with: Connect GitHub repo to Render

services:
  # Django Backend
  - type: web
    name: ltgb-api
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn love_backend.wsgi:application
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: ".onrender.com,.lovethatgivesback.com"
      - key: DATABASE_URL
        fromDatabase:
          name: ltgb-db
          property: connectionString
      - key: FRONTEND_URL
        value: "https://lovethatgivesback.onrender.com"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://lovethatgivesback.onrender.com,https://lovethatgivesback.com"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://lovethatgivesback.onrender.com,https://lovethatgivesback.com"
      - key: STRIPE_SECRET_KEY
        sync: false  # Set manually in dashboard
      - key: STRIPE_WEBHOOK_SECRET
        sync: false  # Set manually in dashboard
      - key: PYTHON_VERSION
        value: "3.12.0"

  # React Frontend
  - type: web
    name: ltgb-frontend
    runtime: static
    buildCommand: cd ../love_frontend && npm install && npm run build
    staticPublishPath: ../love_frontend/dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: ltgb-db
    plan: free
    databaseName: ltgb
    user: ltgb_user
